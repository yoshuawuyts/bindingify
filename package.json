{
  "name": "bindingify",
  "version": "1.0.0",
  "description": "Transform native bindings to be relative to their file",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test"
  },
  "repository": "yoshuawuyts/bindingify",
  "keywords": [
    "browserify",
    "transform",
    "electron"
  ],
  "license": "MIT",
  "dependencies": {
    "falafel": "^2.0.0",
    "through2": "^2.0.3"
  },
  "devDependencies": {
    "browserify": "^13.1.1",
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "standard": "^8.6.0",
    "tape": "^4.6.2"
  }
}
